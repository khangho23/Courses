app.controller("forgotPasswordCtrl", function ($scope, $rootScope, $http, $routeParams) {
    // let code = Math.floor((Math.random() * 10) + 1);

    // $scope.signUp = () => {
    //     console.log(code);
    // }
})